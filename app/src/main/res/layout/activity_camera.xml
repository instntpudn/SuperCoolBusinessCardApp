<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="horizontal"
    android:id="@+id/cameraimage"
    android:background="@mipmap/backgroundcolor"
    android:paddingRight="20dp">

    <ImageView
        android:id="@+id/image_camera"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_marginLeft="500dp"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:background="@mipmap/camerabutton" />

    <ImageButton
        android:layout_width="45dp"
        android:layout_height="45dp"
        android:id="@+id/imageButton"
        android:background="@mipmap/checkmark00_edited2"
        android:layout_alignParentBottom="true"
        android:layout_alignEnd="@+id/image_camera"
        android:layout_marginBottom="30dp"
        android:layout_marginEnd="200dp" />

    <CheckBox
        android:layout_width="45dp"
        android:layout_height="45dp"
        android:id="@+id/checkBox"
        android:layout_alignTop="@+id/imageButton"
        android:layout_toStartOf="@+id/imageButton"
        android:layout_marginEnd="70dp"
        android:background="@mipmap/xmark00_edited"
        android:layout_marginBottom="30dp" />


</RelativeLayout>